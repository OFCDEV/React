import { createContext } from "react";

export const CountContext = createContext(0);
// export const CountContext = createContext({
//     count,setCount
// });

//This works as a teleportor to pass the prop directly